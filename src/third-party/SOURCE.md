# Source Index

This directory has no direct files. It contains the vendored `croaring/`, `fast_float/`, `sqlite/`, and `google_highway/` subtrees, plus `llama_cpp/`, a git submodule tracking https://github.com/ggml-org/llama.cpp (bundled into `libsketch2_llama.a` for the sk_embedder API).
